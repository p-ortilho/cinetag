import styles from './Rodape.module.css';

const Rodape = () => {
    return(
        <footer className={styles.rodape}>
            <p>
                Desenvolvido por <strong>Cainã Portilho</strong>
            </p>
        </footer>
    );
};

export default Rodape;