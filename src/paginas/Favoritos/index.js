import styles from './Favoritos.module.css';

const Favoritos = () => {
    return(
        <div className={styles.favoritos}>
            alguma coisa
        </div>
    );
};

export default Favoritos;